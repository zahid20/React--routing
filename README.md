# Multiple Routing Practice

## Installation

### Clone repo

``` bash
# clone the repo
$ git clone hhttps://github.com/knowaminul/advanced-routing-react.git

# go into app's directory
$ cd advanced-routing-react

# install app's dependencies
$ npm install

# run the app
$ npm start
